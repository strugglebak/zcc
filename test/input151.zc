#include <stdio.h>

void fred() {
  int x= 5;
  printf("testing x\n");
  if (x > 4) return;
  printf("x below 5\n");
}

int bar(int a, int b) {
  // 表达式
  return (a + b);
}

int main() {
  fred();
  printf("bar = %d\n", bar(1, 2));
  return 0;
}
