int main()
{
  int i; char j; long k;

  i= 10; print_int(i);
  j= 20; print_int(j);
  k= 30; print_int(k);

  for (i= 1;   i <= 5; i= i + 1) { print_int(i); }
  for (j= 253; j != 4; j= j + 1) { print_int(j); }
  for (k= 1;   k <= 5; k= k + 1) { print_int(k); }
  return(i);
  print_int(12345);
  return(3);
}
