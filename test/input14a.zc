int main() {
  char  a;
  char *b;
  char  c;

  int   d;
  int  *e;
  int   f;

  a= 18; print_int(a);
  b= &a; c= *b; print_int(c);

  d= 12; print_int(d);
  e= &d; f= *e; print_int(f);
  return(0);
}
